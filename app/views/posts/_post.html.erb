<tr id="post-<%= post.id %>">
<td class="table-titles" data-label="">        
        <%= gravatar_for post.user, size: 50 %>
    <div>
        <span class="badge badge-light"><%= post.tag.name %></span>
      <% if post.title.present?%>
      <%= post.title %>
      <%
=begin%>
 <% elsif post.alumni.name.present?%>
      <%= post.alumni.name %> 
<%
=end%>
     <% elsif post.content.present? %>
    <%= post.content.truncate(30)  %> 
      <%end%>
   
      

    <nav class="table-nav">
        <ul>
            <% if current_user.admin? || current_user?(post.user)%>
            <li><%= link_to "編集", edit_post_path(post) %></li>
            <li><%= link_to "削除", post_path(post), method: :delete,
                                       data: { confirm: "本当に削除しますか？" } %></li>
            <% end %>
        </ul>
    </nav>
    </div>
</td>
<td data-label="作成者"><%= link_to post.user.name, post.user %></td>
<td data-label="作成日"><%= post.created_at.strftime('%Y/%m/%d') %></td>
<td data-label="更新日"><%= post.updated_at.strftime('%Y/%m/%d') %></td>
</tr>